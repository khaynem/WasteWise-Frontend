export const metadata = {
  title: 'Email Verified - WasteWise',
  description: 'Email verification successful',
};

export default function EmailVerifiedLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}
